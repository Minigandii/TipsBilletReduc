<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1">

    <title>{% block title %}Welcome!{% endblock %}</title>
    {# Run composer require symfony/webpack-encore-bundle
           and uncomment the following Encore helpers to start using Symfony UX #}
    {% block stylesheets %}
        {# {{ encore_entry_link_tags('app') }} #}

        <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.2.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-rbsA2VBKQhggwzxH7pPCaAqO46MgnOM80zW1RWuH61DGLwZJEdK2Kadq2F9CUG65" crossorigin="anonymous">
        <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.3.0/css/all.min.css" integrity="sha512-...">
        <script src="https://kit.fontawesome.com/c351f35f93.js" crossorigin="anonymous"></script>



        <style>

            /* Show it is fixed to the top */
            body {
                text-align:center;
                padding-top: 4.5rem;                 
                background-color: #FF4130 !important;
                font-family:'OldschoolGrotesk_W', sans-serif;
                
            }

            .navbar{
                background-color: #FF4130 !important;
            }

            .bd-placeholder-img {
                font-size: 1.125rem;
                text-anchor: middle;
                -webkit-user-select: none;
                -moz-user-select: none;
                user-select: none;
            }

            .modal {
                display: none;
                position: fixed;
                top: 0;
                left: 0;
                width: 100%;
                height: 100%;
                background-color: rgba(0, 0, 0, 0.7);
            }
            
            .modal-content {
                position: absolute;
                top: 50%;
                left: 50%;
                transform: translate(-50%, -50%);
                background-color: #fff;
                padding: 20px;
                border: 1px solid #ccc;
                width:70%;
                text-align: center; /* Centrer le contenu */
            }



            @media (min-width: 768px) {
                .bd-placeholder-img-lg {
                    font-size: 3.5rem;
                }
            }

            @font-face {
                font-family: 'OldschoolGrotesk_W';
                src: url('/font/WOFF2/OldschoolGrotesk_W-Book.woff2') format('woff2');
                /* Vous pouvez spécifier d'autres styles et variantes ici si nécessaire */
            }
        </style>



    {% endblock %}

    {% block javascripts %}
        <script>
        function openHelpModal() {
            var modal = document.getElementById("helpModal");
            modal.style.display = "block";
        }

        // Fonction pour fermer la fenêtre modale d'aide
        function closeHelpModal() {
            var modal = document.getElementById("helpModal");
            modal.style.display = "none";
        }
        </script>
    {% endblock %}
</head>
<body>
        <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.2.3/dist/js/bootstrap.bundle.min.js" integrity="sha384-kenU1KFdBIe4zVF0s0G1M5b4hcpxyD9F7jL+jjXkk+Q2h455rYXK/7HAuoJl+0I4" crossorigin="anonymous"></script>

<main class="container">
    {% block body %}

        <nav class="navbar navbar-expand-md navbar-dark fixed-top bg-dark">
            <div class="container-fluid">
                <a class="navbar-brand" href="#">
                    <img src="{{ asset('images/tip-blanc.png') }}" width="100" alt="Monogramme">
                </a>
                <span class="navbar-text mx-auto text-truncate flex-grow-1" style="max-width: 50%">{{ theatre.nom }}</span>
                <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
                    <span class="navbar-toggler-icon"></span>
                </button>
                <div class="collapse navbar-collapse" id="navbarSupportedContent">
                    <ul class="navbar-nav mr-auto">
                        <li class="nav-item">
                            <a class="nav-link" href="/theatre/editTheatre/{{ theatre.id }}">Modifier nos informations</a>
                        </li> 
                        <li class="nav-item">
                            <a class="nav-link" href="{{ path('app_add_ouvreur') }}">Ajouter un Ouvreur</a>
                        </li>
                        <li class="nav-item">
                            <a class="nav-link" href="{{path('app_create_account_stripe_link')}}">Compte Stripe</a>
                        </li> 

                        <li class="nav-item">
                            <a class="nav-link" href="{{path('app_theatre_view_qr')}}">Qr code</a>
                        </li>

                        <li class="nav-item">
                            <a class="nav-link" href="javascript:void(0);" onclick="openHelpModal()">Aide</a>
                        </li>
                     
                        <li class="nav-item">
                            <a class="nav-link" href="{{path('app_logout')}}">Se deconnecter</a>
                        </li>

                    </ul>
                </div> 
                          
            </div>
        </nav>

        <!-- Fenêtre modale d'aide -->


<div id="helpModal" class="modal" tabindex="-1" role="dialog">
    <div class="modal-content">
      <div class="modal-header">
        <h5 class="modal-title">Vous avez besoin d'aide ?</h5>
      </div>
      <div class="modal-body">
        <p>Contactez Lucie Ayzac au 06 45 74 03 36</p>
      </div>
      <div class="modal-footer">
        <button type="button" class="btn btn-light btn-outline-dark" onclick="closeHelpModal()" data-dismiss="modal">Fermer</button>
      </div>
    </div>
</div>
    

    {% endblock %}
</main>
</body>
</html>
